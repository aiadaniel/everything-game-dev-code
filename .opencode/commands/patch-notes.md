# /patch-notes

Resolve to the shared `commands/patch-notes.md` workflow.

## Adapter behavior
- primary owner: `release-manager`
- generate patch notes from changelog and milestone data
- follow the shared command instructions for invoked agents, required skills, and expected output
