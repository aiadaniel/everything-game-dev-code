# /scene-bootstrap

Resolve to the shared `commands/scene-bootstrap.md` workflow.

## Adapter behavior
- primary owner: `unity-reviewer`
- bootstrap a new scene with proper conventions
- follow the shared command instructions for invoked agents, required skills, and expected output
