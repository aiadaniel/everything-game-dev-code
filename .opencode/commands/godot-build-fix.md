# /godot-build-fix

Resolve to the shared `commands/godot-build-fix.md` workflow.

## Adapter behavior
- primary owner: `godot-build-resolver`
- this command targets Godot only — do not mix other engine guidance
- diagnose and fix Godot build errors
- follow the shared command instructions for invoked agents, required skills, and expected output
