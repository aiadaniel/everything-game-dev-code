# /telemetry-plan

Resolve to the shared `commands/telemetry-plan.md` workflow.

## Adapter behavior
- primary owner: `telemetry-analyst`
- define telemetry events, schemas, and dashboards
- follow the shared command instructions for invoked agents, required skills, and expected output
